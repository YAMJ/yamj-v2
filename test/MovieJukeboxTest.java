import junit.framework.TestCase;


public class MovieJukeboxTest extends TestCase {
	

	public void test1() {
		
	}
	
	private void buildFakeMovieDirectory(String src, String dst) {
		/* File f = new File(src);
		f.getParentFile().mkdirs();
		
	   InputStream in = null;
	   OutputStream out = null; 
		
		if (src.toUpperCase().endsWith("NFO")) {
			   try {
			      in = new FileInputStream(fileToScan);
			      out = new FileOutputStream(f);
			      while (true) {
			         synchronized (buffer) {
			            int amountRead = in.read(buffer);
			            if (amountRead == -1) {
			               break;
			            }
			            out.write(buffer, 0, amountRead); 
			         }
			      } 
			   } catch (IOException e) {
				  e.printStackTrace();
			   } finally {
				   try {
				      if (in != null) {
				         in.close();
				      }
				      if (out != null) {
				         out.close();
				      }
				   } catch(IOException e) {
					   
				   }
			   }
		} else {
			try {
				out= new FileOutputStream(f);
				out.write(1);
				out.close();
		   } catch (IOException e) {
				  e.printStackTrace();
		   } finally {
			   try {
			      if (in != null) {
			         in.close();
			      }
			      if (out != null) {
			         out.close();
			      }
			   } catch(IOException e) {
				   
			   }
		   } 
		} */
	}
}
